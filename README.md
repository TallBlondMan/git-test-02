# A new repository, started via GIT INIT

Time to learn the difference between init and init --bare 

## This is to test -u flag with git push

So there will be another line after this pushed with -u

